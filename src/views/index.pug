html 
    head 
        title WEB-labos1
    body
        if username
            h1 Welcome #{username}
        ul
            if username
                li.links: a(href='/competition') Generiraj natjecanje              
                li: form(action="/logout", method="get")
                    input(type="submit", value="Logout " + username)
            else
                li.links: a(href='/login', title='Prijava') Prijava
